import 'package:flutter/material.dart';

class AppColors {
  // Main background (warm off-white/light cream)
  static const Color background = Color(0xFFF7F6F3);

  // Surface colors
  static const Color cardBackground = Colors.white;
  static const Color bottomNavBarBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF706F6C);
  static const Color textAccent = Color(0xFF3C3C3E); // Caveat handwritten text

  // Dark square boxes for icons/avatars (profile initial)
  static const Color darkBox = Color(0xFF111111);
  static const Color secondaryDarkBox = Color(0xFF2C2C2A);

  // Status/Accent colors
  static const Color border = Color(0xFFE8E7E3);
  static const Color activeIndicator = Color(0xFF111111);
  static const Color inactiveIcon = Color(0xFF9F9E9B);

  // Offline banner colors
  static const Color offlineBannerBg = Color(0xFFF2F0EC);
  static const Color offlineBannerText = Color(0xFF555452);

  // Badge colors
  static const Color badgePaidBg = Color(0xFFF0EFEA);
  static const Color badgePaidText = Color(0xFF4A4A48);
}
